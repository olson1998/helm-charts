apiVersion: v1
kind: Service
metadata:
  name: {{ include "redis-mini.fullname" . }}
  labels:
    {{- include "redis-mini.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "redis-mini.selectorLabels" . | nindent 4 }}
