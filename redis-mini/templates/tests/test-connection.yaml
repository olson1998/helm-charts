apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "redis-mini.fullname" . }}-test-connection"
  labels:
    {{- include "redis-mini.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "redis-mini.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
